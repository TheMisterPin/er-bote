
import { Layout } from '../components/layout/layout'


function StaffPage() {


  return (
    <Layout>
      {/* <h2> El Equipo  </h2>
      {
        employees.length === 0 ? (
          <h4>
            {' '}
            Vaya! Tu equipo esta vacio!
            <ModalButton />
          </h4>

        ) : <ModalButton />
      } */}

    </Layout>
  )
}

export default StaffPage
